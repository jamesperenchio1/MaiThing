packages:
  - "apps/*"
  - "packages/*"

# Keep React 18 (consumer) and React 19 (admin) type trees isolated. Without this,
# pnpm may dedupe peer dependencies across workspaces and pull React 19 types
# into the consumer app, breaking the JSX component type checks.
dedupe-peer-dependents: false
